stepback: true

buildvariants:
    - display_name: Ubuntu 20.04
      name: ubuntu2004
      run_on:
          - ubuntu2004
      tasks:
          - name: SystemFail

tasks:
    - name: SystemFail
      commands:
          - command: git.get_project
            params:
                directory: src
                revisions:
                foo: ${foo_rev}
          - command: shell.exec
            params:
                script: sleep 1000
