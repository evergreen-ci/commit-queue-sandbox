buildvariants:
  - display_name: Test BV 1
    name: bv1
    run_on:
      - ubuntu2004-small
    tasks:
      - name: task1
      - name: task2

tasks:
  - name: task1
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "noop"
  - name: task2
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "noop2"

