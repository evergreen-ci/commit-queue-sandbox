buildvariants:
  - display_name: Test BV 2
    name: bv2
    run_on:
      - ubuntu2004-small
    tasks:
      - name: task3

tasks:
  - name: task3
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "noop2"

