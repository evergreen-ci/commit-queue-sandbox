## this is a changes
ignore:
    - "*.md" # don't schedule tests if a commit only changes markdown files
stepback: true

buildvariants:
    - display_name: Ubuntu 20.04
      name: ubuntu2004
      run_on:
          - ubuntu2004
      tasks:
          - name: task1

tasks:
    - name: task1
      commands:
          - command: shell.exec
            params:
                script: |
                    sleep 100000
