buildvariants:
  - display_name: Test BV 1
    name: bv1
    run_on:
      - ubuntu2004-small
    tasks:
      - name: task1
      - name: task4

tasks:
  - name: task4
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "noo"
  - name: task2
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "noop2"

