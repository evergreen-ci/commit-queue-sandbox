buildvariants:
- display_name: Ubuntu 16.04
  name: ubuntu1604
  run_on:
  - ubuntu1604-test
  tasks:
  - name: unit_tests

functions:
  create virtualenv:
  - command: shell.exec
    params:
      working_dir: src
      script: |
        echo "noop"

pre:
- command: git.get_project
  params:
    directory: src
- func: create virtualenv

post:
- command: attach.xunit_results
  params:
    file: src/junit-*.xml

tasks:
- name: unit_tests
  commands:
  - command: shell.exec
    params:
      working_dir: src
      script: |
        echo "noop"
