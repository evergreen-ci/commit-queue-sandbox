command_type: system

tasks:
  - name: noop
    commands:
      - command: shell.exec
        params:
          shell: bash
          script: echo "module branch"
  - name: one-minute
    commands:
      - command: shell.exec
        params:
          shell: bash
          script: sleep 60

buildvariants:
  - name: test
    display_name: Test
    run_on:
      - ubuntu2204-small
    tasks:
      - name: noop
  - name: mq
    display_name: Merge Queue
    run_on:
      - ubuntu2204-small
    tasks:
      - name: one-minute
