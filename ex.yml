buildvariants:
    - name: binbinbin
      display_name: bin bin bin
      run_on:
        - ubuntu1604-test
      tasks:
        - name: print-stuff
tasks:
  - name: print-stuff
    commands:
      - command: shell.exec
        params:
          working_dir: src
          script: |
            echo "bynnbynn"
